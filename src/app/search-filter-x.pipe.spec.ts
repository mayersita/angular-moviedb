import { SearchFilterXPipe } from './search-filter-x.pipe';

describe('SearchFilterXPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchFilterXPipe();
    expect(pipe).toBeTruthy();
  });
});
